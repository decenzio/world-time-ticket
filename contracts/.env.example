# Example .env for contract deployment
# Replace with your own values and save as .env (do NOT commit private keys)

# RPC URL for the network (e.g. https://rpc.world-testnet.example)
RPC_URL=https://your-rpc-url

# Private key for deployer (hex, no 0x prefix or with 0x depending on forge expectations)
PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Owner address to pass into the constructor
OWNER=0xYourOwnerAddressHere
